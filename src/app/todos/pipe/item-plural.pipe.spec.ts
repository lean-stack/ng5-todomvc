import { ItemPluralPipe } from './item-plural.pipe';

describe('ItemPluralPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemPluralPipe();
    expect(pipe).toBeTruthy();
  });
});
